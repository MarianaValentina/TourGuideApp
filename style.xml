<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/primary_dark_color</item>
    </style>

    <!-- Style for the tab -->
    <style name="CategoryTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">#ff0000</item>
        <item name="tabSelectedTextColor">#000000</item>
        <item name="tabBackground">@color/background</item>
        <item name="tabTextAppearance">@style/CategoryTabTextAppearance</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="CategoryTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">#A8A19E</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- style for the picture of the start page -->
    <style name="Image_Start_Page">
        <item name="android:layout_marginStart">@dimen/margin_start_page</item>
        <item name="android:layout_marginEnd">@dimen/margin_start_page</item>
    </style>

    <!--style for the text of the start page -->
    <style name="Text_Start_Page">
        <item name="android:textColor">#000000</item>
        <item name="android:textStyle">italic</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_marginStart">@dimen/margin_start_page</item>
        <item name="android:layout_marginEnd">@dimen/margin_start_page</item>
    </style>

    <!-- style for the pictures of the views -->
    <style name="Image_Views">
        <item name="android:layout_marginStart">@dimen/Image_Views</item>
        <item name="android:layout_marginTop">@dimen/Image_Views</item>
    </style>

    <!-- Style for a category of vocabulary words -->
    <style name="CategoryStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/list_item_height</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:padding">16dp</item>
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
    </style>
</resources>
